#include "main.h"
/**
 * mul - function that checks for a digit of ( 0 through)
 * @a: a - variable 
 * @b: b - variable
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
