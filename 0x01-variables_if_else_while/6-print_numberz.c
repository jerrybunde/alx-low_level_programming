#include <stdio.h>
/**
 * main - Prints numbers from 0 to 9
 * Return: Always 0(Success)
 */
int main(void)
{
	int alphabets = 0;

	while (alphabets <= 0)
	{
		putchar(alphabets);
		alphabets++;
	}
	putchar('\n');
	return (0);
}
